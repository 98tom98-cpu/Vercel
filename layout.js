export const metadata = {
  title: "Carga de tickets",
  description: "Interfaz para cargar información de tickets de compra sin guardar datos.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="es">
      <body>{children}</body>
    </html>
  );
}
